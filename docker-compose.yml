version: '3.9'
services:
  postgres:
    networks:
      - internal
    env_file:
      - .env
    image: postgres:15
    container_name: postgres_dev
    ports:
      - "5455:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
